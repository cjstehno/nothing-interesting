# word-counter

A Python command-line app built with Click that prompts for a string and prints the number of words entered.

## Setup (uv)

```bash
uv sync
```

## Run

```bash
uv run word-counter
```

You will be prompted to enter a string of words. After pressing Enter, the app prints the word count.
